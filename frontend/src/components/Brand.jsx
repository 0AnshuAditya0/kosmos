export function Mark({ inverse = false }) { return <span className={`mark ${inverse ? "mark-inverse" : ""}`} aria-hidden="true"><i /><i /><i /></span>; }
export function Header({ view, setView, landing = false, NAV }) { return <header className={`site-header ${landing ? "landing-header" : ""}`}><button className="brand" onClick={() => setView("landing")} aria-label="Go to Kosmos home"><Mark inverse={landing} /><span>Kosmos<span className="brand-dot">/</span></span></button>{!landing && <nav aria-label="Primary navigation">{NAV.map((item) => <button className={view === item ? "nav-active" : ""} key={item} onClick={() => setView(item)}>{item}</button>)}</nav>}{landing ? <button className="header-link" onClick={() => setView("Ask")}>Open workspace <span>↗</span></button> : <span className="system-pill"><b /> API connected</span>}</header>; }
export function AbstractOrbit() { return <div className="orbit-art" aria-hidden="true"><div className="orbit orbit-a" /><div className="orbit orbit-b" /><div className="orbit orbit-c" /><div className="orbit-core"><Mark /></div><span className="orbit-label label-a">VOICE</span><span className="orbit-label label-b">SOURCE</span><span className="orbit-label label-c">TRUST</span></div>; }
