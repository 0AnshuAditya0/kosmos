# Required only for the /ask voice endpoint.
SARVAM_API_KEY=replace_with_a_rotated_key

# Optional. Comma-separated origins for a separately deployed frontend.
# Leave as * when the frontend is served by this same FastAPI application.
ALLOWED_ORIGINS=*
